- defaults:
    link:
        relink: true
        force: true

- clean: ['~']

- link:
    ~/.bashrc: .bashrc
    ~/.hushlogin: .hushlogin
    ~/.profile: .profile
    ~/.config/:
        create: true
        glob: true
        path: config/*
    ~/run: run.sh
    ~/.gitconfig: .gitconfig
    
